import javax.swing.*;

public class Hangman extends JFrame{
  public Hangman(){
      super("Hangman Game (Java Ed.)");
      setSize(CommenConstants.FRAME_SIZE);
      setDefaultCloseOperation(EXIT_ON_CLOSE);
      setLocationRelativeTo(null);
      setLayout(null);
      setResizable(false);
    }

}
